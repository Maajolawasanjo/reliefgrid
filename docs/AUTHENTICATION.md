# 🔒 Authentication & JWT Specification

ReliefGrid implements standard OAuth2 Bearer Token authentication with signed JSON Web Tokens (JWT) protecting all private endpoints (`/analytics/*`, `/memories/*`, `/telemetry/*`).
