# 🔍 Vector Search Engine Specification

ReliefGrid leverages `pgvector` in CockroachDB to perform sub-second 1024-dimensional semantic similarity vector lookups using Cosine Distance (`<=>`) and HNSW index structures.
